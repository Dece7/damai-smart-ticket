# 大麦智能票务助手 — 使用说明书

> 基于 Python + LangChain + LangGraph 的 AI 智能客服系统

---

## 一、项目简介

大麦智能票务助手是一个基于大模型的智能客服系统，支持演出查询、购票下单、退票政策问答等功能。系统提供三种对话模式和多 Agent 协作模式，并支持 MCP 协议供外部客户端调用。

### 技术栈

| 层级 | 技术 |
|------|------|
| 后端 | Python 3.11 / FastAPI / LangChain / LangGraph |
| 向量库 | ChromaDB + BM25 混合检索 + Reranker 精排 |
| 前端 | Vue 3 / TypeScript / Vite / Element Plus / ECharts |
| 模型 | Mimo（对话/Function Calling）/ DeepSeek（Embedding/改写） |
| 协议 | MCP（Model Context Protocol）|

---

## 二、环境要求

| 依赖 | 版本 |
|------|------|
| Python | 3.11+ |
| Node.js | 20+（仅前端开发需要） |
| 操作系统 | Windows / macOS / Linux |

---

## 三、快速启动

### 3.1 启动后端服务

```bash
cd D:\ClaudeCodeProjects\CCAIAgent\damai-smart-ticket
.venv\Scripts\python.exe run.py
```

启动成功后：
- 浏览器访问 `http://localhost:8000` — 对话主界面
- 访问 `http://localhost:8000/admin` — 管理仪表盘
- 访问 `http://localhost:8000/knowledge` — 知识库管理
- 访问 `http://localhost:8000/scalar` — API 文档

### 3.2 前端开发模式（可选）

如果需要修改前端代码：

```bash
cd D:\ClaudeCodeProjects\CCAIAgent\damai-smart-ticket\frontend
npm run dev
```

开发时访问 `http://localhost:5173`，支持热更新（修改代码自动刷新）。

> **注意：** 大多数时候不需要启动前端开发模式。直接用 `python run.py` 启动后端即可，前端已编译到 `static/` 目录。

### 3.3 启动 MCP Server（可选）

MCP Server 供 Claude Desktop、Cursor 等外部客户端调用项目的 9 个工具。

```bash
# stdio 模式（本地客户端连接）
.venv\Scripts\python.exe -m app.mcp.server

# HTTP 模式（远程客户端连接，默认端口 8001）
.venv\Scripts\python.exe app\mcp\server.py --http --port 8001
```

---

## 四、功能说明

### 4.1 四种对话模式

| 模式 | 说明 | 适用场景 |
|------|------|---------|
| **Agent** | 单 Agent + 9 个工具，自动判断意图选择工具 | 通用场景 |
| **多 Agent** | Supervisor 调度，票务专家 + 知识库专家协作 | 复杂问题（如"查演唱会+问退票政策"） |
| **贴心助手** | Function Calling，直接调用业务工具 | 查节目、查票档、下单 |
| **规则助手** | RAG 检索知识库，基于文档回答 | 退票政策、订票流程、入场规则 |

### 4.2 9 个功能工具

| 工具 | 功能 | 示例 |
|------|------|------|
| `search_program` | 搜索演出信息 | "北京有什么演唱会" |
| `get_program_detail` | 查节目详情 | "周杰伦演唱会什么时候" |
| `get_ticket_info` | 查票档信息 | "680的票还有吗" |
| `create_order` | 创建订单 | "帮我买两张680的票" |
| `query_ticket_status` | 查实时余票 | "还有多少票" |
| `check_order_status` | 查订单状态 | "我上次订的票到哪了" |
| `calculate_price` | 计算票价含折扣 | "两张680会员打几折" |
| `get_recommendations` | 推荐演出 | "有什么好看的演唱会" |
| `search_knowledge_base` | 检索知识库 | "怎么退票" |

### 4.3 知识库

知识库包含 12 份 Markdown 文档，覆盖完整票务业务场景：

| 文档 | 覆盖内容 |
|------|---------|
| 节目订票-相关问题与回答 | 购票流程、缺货登记、儿童票 |
| 节目取消和退票-相关问题与回答 | 退票政策、退款流程、退票手续费 |
| 演出变更与延期-相关问题与回答 | 演出取消/延期/场地变更处理 |
| 选座指南-相关问题与回答 | 在线选座、座位类型、选座注意事项 |
| 支付方式-相关问题与回答 | 支付方式、支付失败、发票 |
| 会员权益-相关问题与回答 | 会员等级、折扣、积分 |
| 优惠活动-相关问题与回答 | 早鸟票、学生票、优惠券 |
| 各城市场馆信息-相关问题与回答 | 北京/上海/深圳等城市场馆 |
| 入场须知-相关问题与回答 | 入场证件、安检、禁带物品 |
| 无障碍服务-相关问题与回答 | 轮椅服务、助听设备、导盲犬 |
| 儿童票政策-相关问题与回答 | 儿童票规则、婴儿入场 |
| 取票与配送-相关问题与回答 | 电子票、纸质票、转赠、补票 |

### 4.4 知识库管理

访问 `http://localhost:8000/knowledge`：

- **查看文档列表** — 显示所有知识库文档及大小
- **上传文档** — 上传 .md 格式的 Markdown 文件
- **删除文档** — 删除指定文档
- **重建索引** — 上传/删除后需重建索引才能生效

### 4.5 管理仪表盘

访问 `http://localhost:8000/admin`：

- **总览卡片** — 总对话数、总消息数、总 Token、平均 Token
- **趋势图** — 每日 Token 消耗趋势（输入/输出堆叠）
- **饼图** — 三种模式（贴心助手/规则助手/Agent）Token 占比

---

## 五、API 接口

### 对话接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/chat` | POST | 贴心助手/规则助手（SSE 流式） |
| `/api/agent` | POST | Agent 模式（SSE 流式） |
| `/api/agent/multi` | POST | 多 Agent 协作（SSE 流式） |

### 会话管理

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/conversations` | GET | 获取对话列表 |
| `/api/conversations/{id}/messages` | GET | 获取对话消息 |
| `/api/conversations/{id}` | DELETE | 删除对话 |
| `/api/conversations/{id}/title` | PUT | 修改标题 |
| `/api/conversations/{id}/pin` | POST | 切换置顶 |
| `/api/conversations/{id}/generate-title` | POST | 自动生成标题 |

### 知识库管理

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/knowledge/documents` | GET | 获取文档列表 |
| `/api/knowledge/upload` | POST | 上传文档 |
| `/api/knowledge/documents/{name}` | DELETE | 删除文档 |
| `/api/knowledge/rebuild` | POST | 重建索引 |

### 管理统计

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/admin/stats/summary` | GET | 总览统计 |
| `/api/admin/stats/trend` | GET | 每日趋势 |
| `/api/admin/stats/by_mode` | GET | 模式用量 |

### MCP 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/mcp/info` | GET | MCP Server 信息 |
| `/api/mcp/tools` | GET | MCP 工具列表 |

---

## 六、目录结构

```
damai-smart-ticket/
├── app/                        # Python 后端
│   ├── api/                    # API 路由
│   │   ├── chat.py             # 对话接口
│   │   ├── agent.py            # Agent 接口
│   │   ├── conversation.py     # 会话管理
│   │   ├── knowledge.py        # 知识库管理
│   │   ├── admin.py            # 管理统计
│   │   └── mcp.py              # MCP 接口
│   ├── chains/                 # AI 链
│   │   ├── agent.py            # 单 Agent
│   │   ├── multi_agent.py      # 多 Agent 协作
│   │   └── tools.py            # 工具定义（9 个）
│   ├── core/                   # 核心模块
│   │   ├── config.py           # 配置
│   │   ├── database.py         # 数据库
│   │   ├── prompts.py          # 提示词
│   │   └── security.py         # 安全防护
│   ├── mcp/                    # MCP 模块
│   │   └── server.py           # MCP Server
│   ├── models/                 # 数据模型
│   ├── pipelines/              # 处理管线
│   │   ├── document_pipeline.py # 文档处理
│   │   └── rag_pipeline.py     # RAG 检索
│   ├── services/               # 业务服务
│   └── main.py                 # FastAPI 入口
├── frontend/                   # Vue 3 前端
│   ├── src/
│   │   ├── api/                # API 封装
│   │   ├── components/         # 组件
│   │   ├── router/             # 路由
│   │   ├── stores/             # 状态管理
│   │   ├── styles/             # 样式
│   │   └── views/              # 页面
│   ├── vite.config.ts          # Vite 配置
│   └── package.json
├── docs/rag/                   # 知识库文档（12 份）
├── chroma_db/                  # 向量数据库
├── data/                       # BM25 索引等
├── eval/                       # 评估脚本和数据集
├── static/                     # 前端构建产物
├── run.py                      # 启动脚本
├── build_rag.py                # 知识库构建脚本
├── requirements.txt            # Python 依赖
└── docker-compose.yml          # Docker 部署
```

---

## 七、常见问题

### Q: 启动报错 "No module named xxx"

```bash
# 安装依赖
.venv\Scripts\python.exe -m pip install -r requirements.txt
# 或使用 uv
uv pip install -r requirements.txt
```

### Q: 前端页面空白

检查 `static/` 目录下是否有 `index.html` 和 `assets/` 目录。如果没有：

```bash
cd frontend
npm install
npm run build
```

### Q: 知识库问答返回"暂未找到相关信息"

1. 检查 `docs/rag/` 目录下是否有 .md 文件
2. 重建索引：访问 `http://localhost:8000/knowledge`，点击"重建索引"
3. 或命令行执行：
   ```bash
   .venv\Scripts\python.exe build_rag.py
   ```

### Q: MCP Server 连接不上

1. 确认 MCP Server 已启动
2. stdio 模式：在 Claude Desktop 的配置文件中添加：
   ```json
   {
     "mcpServers": {
       "damai-ticket": {
         "command": "D:\\ClaudeCodeProjects\\CCAIAgent\\damai-smart-ticket\\.venv\\Scripts\\python.exe",
         "args": ["-m", "app.mcp.server"],
         "cwd": "D:\\ClaudeCodeProjects\\CCAIAgent\\damai-smart-ticket"
       }
     }
   }
   ```

### Q: 端口被占用

```bash
# 查看占用端口的进程
netstat -ano | findstr :8000
# 杀掉进程
taskkill /F /PID <PID>
```

---

## 八、Docker 部署

```bash
# 构建并启动
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止
docker-compose down
```

---

## 九、开发命令速查

| 命令 | 说明 |
|------|------|
| `.venv\Scripts\python.exe run.py` | 启动后端服务 |
| `cd frontend && npm run dev` | 启动前端开发 |
| `cd frontend && npm run build` | 构建前端 |
| `.venv\Scripts\python.exe build_rag.py` | 重建知识库索引 |
| `.venv\Scripts\python.exe -m app.mcp.server` | 启动 MCP Server |
| `.venv\Scripts\python.exe eval\evaluate.py` | 运行评估 |
| `.venv\Scripts\python.exe eval\evaluate.py --compare` | 对比评估 |
| `.venv\Scripts\python.exe eval\evaluate.py --rewrite` | 查询改写评估 |
